{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node --require ./scripts/ipv4-dns.js index.js",
    "dev": "nodemon --require ./scripts/ipv4-dns.js index.js",
    "migrate": "NODE_OPTIONS='--require ./scripts/ipv4-dns.js' npx sequelize-cli db:migrate",
    "migrate:undo": "NODE_OPTIONS='--require ./scripts/ipv4-dns.js' npx sequelize-cli db:migrate:undo",
    "migrate:undo:all": "NODE_OPTIONS='--require ./scripts/ipv4-dns.js' npx sequelize-cli db:migrate:undo:all",
    "seed": "NODE_OPTIONS='--require ./scripts/ipv4-dns.js' npx sequelize-cli db:seed:all",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Promise",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cloudinary": "^2.10.0",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.20.0",
    "pg-hstore": "^2.3.4",
    "postgres": "^3.4.9",
    "psql": "^0.0.1",
    "rate-limit-redis": "^4.2.0",
    "redis": "^4.7.0",
    "sequelize": "^6.37.8",
    "sequelize-cli": "^6.6.5",
    "socket.io": "^4.8.3"
  }
}
